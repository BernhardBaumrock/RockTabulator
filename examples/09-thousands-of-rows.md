By default there are 200k pages created under `/data`. You can create more pages using the `/site/assets/scripts/create.php` script from the commandline:

```bash
cd site/assets/scripts
php -f ./create.php
```
